// completeWork —— Fiber 树构建的"归"阶段

import {Fiber} from "./ReactInternalTypes";

// 完成 Fiber 节点的工作，创建/更新 DOM 节点（待实现）
export function completeWork(
    current: Fiber | null,
    workInProgress: Fiber
): Fiber | null {
    return null
}